from django.urls import path

from . import views

app_name = 'page'

urlpatterns = [
	# path('', views.ConvertView.as_view(), name='start'),
	# path('success/<uuid:pk>', views.SuccessView.as_view(), name='success'),
	# path('CleanUp/<uuid:pk>', views.cleanup, name='cleanup'),
]